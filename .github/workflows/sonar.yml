name: SonarCloud Analysis

on:
  push:
    branches: ["main"]
  pull_request:

jobs:

  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Prepare TestResults folder
        run: mkdir -p TestResults

      - name: Test with coverage (inside test project)
        working-directory: GadoApi.Tests
        run: |
          mkdir -p TestResults
          dotnet test GadoApi.Tests.csproj \
            --configuration Release \
            --logger "trx;LogFileName=../TestResults/TestResults.trx" \
            /p:CollectCoverage=true \
            /p:CoverletOutputFormat=opencover \
            /p:CoverletOutput=../TestResults/coverage.opencover.xml

      - name: Upload coverage
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: TestResults/coverage.opencover.xml

  sonarcloud:
    needs: build
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
        
      - name: Download coverage
        uses: actions/download-artifact@v4
        with:
          name: coverage
          path: TestResults
        
      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@v2
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.organization=pedrofonseca1227
            -Dsonar.projectKey=pedrofonseca1227_GadoApi
            -Dsonar.sources=GadoApi
            -Dsonar.tests=GadoApi.Tests
            -Dsonar.test.inclusions=GadoApi.Tests/**/*.cs
            -Dsonar.cs.opencover.reportsPaths=TestResults/coverage.opencover.xml
            -Dsonar.cs.vstest.reportsPaths=TestResults/TestResults.trx
            -Dsonar.exclusions=**/bin/**,**/obj/**

